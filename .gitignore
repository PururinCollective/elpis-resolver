# Build output -- everything the build produces goes in bin/
*.o
*.d
/bin/

# Local overrides
/elpis.local.conf
core
core.*

# generated by make: the commit the binary was built from, and the flags it
# was built with
src/gitrev.h
src/licence_issuer.stamp
src/cflags.stamp
